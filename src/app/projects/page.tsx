// This page has been removed as it's no longer needed.
// The project list on the homepage now handles showing all projects.
// Navigating to /projects will no longer work as intended.
export default function AllProjectsPage() {
  return null;
}
