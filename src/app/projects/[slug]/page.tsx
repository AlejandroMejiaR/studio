
// This page has been removed as it's no longer needed.
// The project detail view on the homepage now handles showing project details.
// Navigating to /projects/[slug] will no longer work as intended.
export default function ProjectPage({ params }: { params: { slug: string } }) {
  return null;
}
